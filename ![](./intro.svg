<!DOCTYPE html>
<html>
<body>
<button onclick="typeWriter(txt1, txt2)">Click me</button>

<p id="demo"></p>

<script>
let lastTimeFlag = 0; //Required to end recursion at Unwriter
var currentChar = 0;
var txt1= "Can\'t help yourself when it comes to testing?";
var txt2 = "Nor can I. That's how robust systems are made, after all."
var speed = 50;

//OK, this thing ain't beautiful, it was not built to be modular this time. 
//Too many books enqueued awaiting to be read this week. It was an emergency :P
//It receives two strings, which shall be sequentially printed with a "typing effect".

function typeWriter(text, text2) {
  if (currentChar < text.length) {
    document.getElementById("demo").innerHTML += text.charAt(currentChar);
    currentChar++;
	//recursive call after.05 secs
    setTimeout(()=> {typeWriter(text,text2)}, speed); 
  } else {
	//Text printed. Starts deleting after 1 second.
    setTimeout(()=> {Unwriter(text, text2)}, 1000);
  }
}

function Unwriter(text, next = "") {
  if (text.length > 0) {
  	text = text.slice(0,-1);
    document.getElementById("demo").innerHTML = text;
    setTimeout(()=> {Unwriter(text)}, speed);
  } else {
	lastTimeFlag += 1;
    	if (lastTimeFlag == 1){
        currentChar = 0;
        setTimeout(()=> {typeWriter(txt2)}, 750)
        }
  }

}

</script>
</body>
</html>

